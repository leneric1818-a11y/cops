# SLURM Templates

- Templates live in `slurm/templates/`.
- Generated job files are written to `slurm/generated/`.
- Use `python -m scripts.generate_slurm` to render a template from cluster + experiment configs.
